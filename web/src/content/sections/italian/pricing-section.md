---
enable: true # Control the visibility of this section across all pages where it is used
badge: "Our Pricing Plans"
title: "Simple, Transparent And Great pricing."
decorativeImage: "/images/decorative/shape/shape-8.png"
decorativeImageAlt: "Pricing Background"

list:
  - enable: true
    name: "Basic"
    description: "Duis aute irure dolor in reprehenderit voluptate velit esse cillum dolore eu fugiat"
    price:
      prependValue: "$"
      value: "44.9"
      appendValue: "/ Month"
    features:
      - Free Consultation
      - Discount Service
      - Feature Planing
      - 24/7 Full Support
    button:
      # Refer to the `sharedButton` schema in `src/sections.schema.ts` for all available configuration options (e.g., enable, label, url, hoverEffect, variant, icon, tag, rel, class, target, etc.)
      enable: true
      label: "Purchase Now"
      url: "/contact/"
      # hoverEffect: "" # Optional: text-flip | creative-fill | magnetic | magnetic-text-flip
      # variant: "" # Optional: fill | outline | text | circle
      # rel: "" # Optional
      # target: "" # Optional
  - enable: true
    name: "Professional"
    description: "Duis aute irure dolor in reprehenderit voluptate velit esse cillum dolore eu fugiat"
    price:
      prependValue: "$"
      value: "54.9"
      appendValue: "/ Month"
    features:
      - Free Consultation
      - Discount Service
      - Feature Planing
      - 24/7 Full Support
    button:
      # Refer to the `sharedButton` schema in `src/sections.schema.ts` for all available configuration options (e.g., enable, label, url, hoverEffect, variant, icon, tag, rel, class, target, etc.)
      enable: true
      label: "Purchase Now"
      url: "/contact/"
      # hoverEffect: "" # Optional: text-flip | creative-fill | magnetic | magnetic-text-flip
      # variant: "" # Optional: fill | outline | text | circle
      # rel: "" # Optional
      # target: "" # Optional
  - enable: true
    name: "Enterprise"
    description: "Duis aute irure dolor in reprehenderit voluptate velit esse cillum dolore eu fugiat"
    price:
      prependValue: "$"
      value: "64.9"
      appendValue: "/ Month"
    features:
      - Free Consultation
      - Discount Service
      - Feature Planing
      - 24/7 Full Support
    button:
      # Refer to the `sharedButton` schema in `src/sections.schema.ts` for all available configuration options (e.g., enable, label, url, hoverEffect, variant, icon, tag, rel, class, target, etc.)
      enable: true
      label: "Purchase Now"
      url: "/contact/"
      # hoverEffect: "" # Optional: text-flip | creative-fill | magnetic | magnetic-text-flip
      # variant: "" # Optional: fill | outline | text | circle
      # rel: "" # Optional
      # target: "" # Optional
---
